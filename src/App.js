import React from "react";
import { TodosListContainer } from "./containers";

const App = props => {
  return (
    <div id="app">
      <TodosListContainer />
    </div>
  );
};

export default App;
